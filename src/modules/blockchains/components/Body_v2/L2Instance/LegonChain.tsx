'use client';

import { Flex } from '@chakra-ui/react';

const LegonChain = () => {
  return (
    <Flex
      mt={['10px', '18px', '28px']}
      w={'100%'}
      minH={'350px'}
      bgColor={'#ECECEC'}
    ></Flex>
  );
};

export default LegonChain;
