import { IAirdrop } from '@/services/api/dapp/airdrop/interface';

export const parseAirdrop = (token: IAirdrop) => {
  const result = {} as DappModel;

  console.log('parseAirdrop', result);

  return result;
};
