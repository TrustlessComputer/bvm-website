'use client';

import { Box } from '@chakra-ui/react';
import s from './styles.module.scss';

const RoadmapModule = () => {
  return <Box className={s.container} bgColor={'#fff'}></Box>;
};

export default RoadmapModule;
