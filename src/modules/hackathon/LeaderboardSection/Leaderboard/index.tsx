import React from 'react';
import s from './Leaderboard.module.scss';

type Props = {};

const Leaderboard = (props: Props) => {
  return <div className={s.wrapper}>Leaderboard</div>;
};

export default Leaderboard;
