import { IRollupL2Info } from '@/services/api/dapp/rollupl2/interface';
import React from 'react';

const ActiveAddress = ({ data }: { data: IRollupL2Info }) => {
  return <div>ActiveAddress</div>;
};

export default ActiveAddress;
