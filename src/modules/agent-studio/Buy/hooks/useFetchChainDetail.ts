import { IModelCategory } from '@/types/customize-model';

const useFetchChainDetail = () => {
  const setTemplate = (template: IModelCategory[]) => {};

  return { setTemplate };
};

export default useFetchChainDetail;
