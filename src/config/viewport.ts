import { Viewport } from 'next';

const viewPortConfig: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  viewportFit: 'cover',
  themeColor: '#000000',
};

export default viewPortConfig;
